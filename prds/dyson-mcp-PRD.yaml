# DYSON-MCP - MCP PRD
# Generated by GRIMLOCK Design Wizard - 2025-12-26

version: "1.0"

project:
  name: "dyson-mcp"
  sdk: "typescript"
  node_version: "18+"

integration:
  service_name: "Dyson Cloud API"
  auth_type: "oauth2"
  # api_docs_url: (not provided)

tools:
  - name: "get_device_status"
    description: "Get current status of a Dyson device"
    parameters:
      - name: "device_id"
        type: "string"
        required: false
        description: "Device ID (optional, defaults to first device)"
    returns: "Device status object with power state, fan speed, oscillation, air quality"
  - name: "set_fan_speed"
    description: "Set the fan speed (1-10 or auto)"
    parameters:
      - name: "device_id"
        type: "string"
        required: false
        description: "Device ID (optional, defaults to first device)"
      - name: "speed"
        type: "string"
        required: true
        description: "Fan speed: 1-10 or 'auto'"
    returns: "Updated device status"
  - name: "set_oscillation"
    description: "Enable or disable oscillation"
    parameters:
      - name: "device_id"
        type: "string"
        required: false
        description: "Device ID (optional, defaults to first device)"
      - name: "enabled"
        type: "boolean"
        required: true
        description: "true to enable, false to disable"
    returns: "Updated device status"
  - name: "get_air_quality"
    description: "Get current air quality readings"
    parameters:
      - name: "device_id"
        type: "string"
        required: false
        description: "Device ID (optional, defaults to first device)"
    returns: "Air quality data (PM2.5, PM10, VOC, NO2, humidity, temperature)"
  - name: "set_night_mode"
    description: "Enable or disable night mode"
    parameters:
      - name: "device_id"
        type: "string"
        required: false
        description: "Device ID (optional, defaults to first device)"
      - name: "enabled"
        type: "boolean"
        required: true
        description: "true to enable, false to disable"
    returns: "Updated device status"

context_efficiency:
  tool_count: 5
  estimated_tokens: 2550
  level: "optimal"
  scope_recommendation: "project"

design_origin:
  source: "form_wizard"
  interface: "n8n_form"
  design_date: "2025-12-26"

